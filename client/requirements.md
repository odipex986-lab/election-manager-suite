## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Icons

## Notes
- The application uses Shadcn UI components extensively.
- Authentication relies on the `/api/me` endpoint.
- All endpoints map to the `api` object in `@shared/routes`.
